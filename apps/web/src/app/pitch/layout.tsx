/**
 * Standalone layout for /pitch — no auth, no AppShell, no sidebar.
 * This page is publicly accessible for potential buyers.
 */
export default function PitchLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
