[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "cd backend && pnpm exec ts-node-dev --transpile-only --no-notify src/main.ts"
restartPolicyType = "on_failure"
