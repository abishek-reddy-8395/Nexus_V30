[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "cd backend && pnpm exec ts-node --transpile-only src/main.ts"
restartPolicyType = "on_failure"

[deploy.environmentVariables]
NODE_ENV = "production"
