[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && cd backend && npx tsc --noEmitOnError false --skipLibCheck || true && cd .."

[deploy]
startCommand = "node backend/dist/main.js"
restartPolicyType = "on_failure"
